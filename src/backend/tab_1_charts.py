from backend.plotting_functions.make_pieplot import pieplot
from backend.plotting_functions.make_barplot import barplot
from backend.plotting_functions.make_lineplot import lineplot
from backend.plotting_functions.make_scatterplot import scatterplot


def make_chart(plot_name, sy, sm, ey, em, additional_settings):
    # x = [2000, 3000, 6000]
    # y = [4000, 6000, 2000]
    # point_labels = ["a", "b", "c"]
    # xlabel = "Wydatki"
    # ylabel = "Przychody"
    #
    # fig = scatterplot(x, y, point_labels, xlabel, ylabel)
    #
    # # ---
    # x_values_list = [[2, 3, 4], [2, 3, 4]]
    # y_values_list = [[2, 6, 5], [4, 1, 3]]
    # names_list = ["jeden", "dwa"]
    # modes_list = ["lines+markers", "lines"]
    # linestyles_list = ["solid", "dash"]
    # x_labels = ['dwa', 'czy', 'cztery']
    # x_tickvals = (2, 3, 4)
    # ylims = (0, 7)
    #
    # fig = lineplot(x_values_list, y_values_list, names_list, modes_list, linestyles_list,
    #                x_labels, x_tickvals, ylims)

    # ---

    return fig
